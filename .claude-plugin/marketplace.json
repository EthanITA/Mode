{
  "$schema": "https://code.claude.com/schemas/marketplace.json",
  "name": "mode",
  "description": "The mode plugin on its own. One entry, so adding this marketplace adds nothing you did not ask for.",
  "owner": {
    "name": "Marco Dong"
  },
  "plugins": [
    {
      "name": "mode",
      "source": "./",
      "description": "Two slots per session, the way you work and the way the assistant talks, switched deterministically before the model reads your message."
    }
  ]
}
