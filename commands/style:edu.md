---
description: Teach it top down, in plain words, carried by pictures rather than prose.
argument-hint: ""
disable-model-invocation: true
---

A hook read this message and set the style slot to `edu` before it reached you, so do not run
`mode style set` over it. The contract is in your context above.

Confirm the switch in one line, then work the way it asks.
