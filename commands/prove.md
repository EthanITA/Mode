---
description: Nothing is claimed working until a real channel says so, run before and after the change.
argument-hint: ""
disable-model-invocation: true
---

A hook read this message and set the mode slot to `prove` before it reached you, so do not run
`mode mode set` over it. The contract is in your context above.

Confirm the switch in one line, then work the way it asks.
