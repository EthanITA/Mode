---
description: The user wants X and is away. Every decision is Claude's, one report waits.
argument-hint: ""
disable-model-invocation: true
---

A hook read this message and set the mode slot to `autopilot` before it reached you, so do not run
`mode mode set` over it. The contract is in your context above.

Confirm the switch in one line, then work the way it asks.
