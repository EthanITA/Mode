---
description: The user is in a hurry. Do the one thing and say done.
argument-hint: ""
disable-model-invocation: true
---

A hook read this message and set the style slot to `fast` before it reached you, so do not run
`mode style set` over it. The contract is in your context above.

Confirm the switch in one line, then work the way it asks.
