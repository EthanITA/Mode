---
description: Work out what to test and how to reach it, run it for real, and report a verdict without fixing anything.
argument-hint: ""
disable-model-invocation: true
---

A hook read this message and set the mode slot to `tester` before it reached you, so do not run
`mode mode set` over it. The contract is in your context above.

Confirm the switch in one line, then work the way it asks.
