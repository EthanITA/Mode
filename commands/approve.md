---
description: Record a yes on a named spec so dispatch can start
argument-hint: <slug>
disable-model-invocation: true
---

The approval of `$1` is already recorded. A hook read this message before it reached you and wrote it down, so there is nothing left for you to run and no file for you to touch.

Say in one line that `$1` is approved and that dispatch is open now. Then pick up whatever the mode's next gate is.

If the argument came through empty, nothing was recorded. Ask which spec is being approved, and stop there.
