---
description: Loop until it is truly done, verified for real and audited clean, twice in a row.
argument-hint: ""
disable-model-invocation: true
---

A hook read this message and set the mode slot to `goal` before it reached you, so do not run
`mode mode set` over it. The contract is in your context above.

Confirm the switch in one line, then work the way it asks.
