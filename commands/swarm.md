---
description: A gateway, not a builder. Route every ask to the owner of that domain, and hire one when none fits.
argument-hint: ""
disable-model-invocation: true
---

A hook read this message and set the mode slot to `swarm` before it reached you, so do not run
`mode mode set` over it. The contract is in your context above.

Confirm the switch in one line, then work the way it asks.
