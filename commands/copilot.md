---
description: "Refine it together, then a team builds it while the user watches."
argument-hint: ""
disable-model-invocation: true
---

A hook read this message and set the mode slot to `copilot` before it reached you, so do not run
`mode mode set` over it. The contract is in your context above.

Confirm the switch in one line, then work the way it asks.
